\ProvidesPackage{preamble}

%% Language and font encodings
\usepackage[english]{babel}
\usepackage[utf8x]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{mathptmx}

% Remove all hyphenation - doesn't work well
% \hyphenpenalty=10000
% \exhyphenpenalty=10000

%% Sets page size and margins
\usepackage[a4paper,top=2cm,bottom=2cm,left=3.5cm,right=3.5cm,marginparwidth=3cm]{geometry}

%% Useful packages
\usepackage{afterpage}
\usepackage{amsmath}
\usepackage[toc,page]{appendix}
\usepackage{caption}
\usepackage{color}
\usepackage{enumitem}
\usepackage{fancybox}
\usepackage{graphicx}
\usepackage{hvfloat}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{listings}
\usepackage{lscape}
\usepackage{multirow}
\usepackage{pdflscape}

% Lets you use the \includepdf command to include a pdf in your document as if it were part of the document
\usepackage{pdfpages}

\usepackage{pgfgantt}
\usepackage{pgfplots}

% Place all figures in the section they were defined in
% Can also use \FloatBarrier to define a barrier
\usepackage[section]{placeins}

\usepackage{rotating}
\usepackage{tabularx}

\usepackage{tikz}
\usetikzlibrary{arrows,shapes,positioning,shadows,trees}

\usepackage{url}
\usepackage{xcolor}
\usepackage{xparse}


\hypersetup{
    colorlinks = false
}

%% Section Numbering
%\setcounter{secnumdepth}{0} % no sections will be numbered
%\setcounter{secnumdepth}{1} % only sections will be numbered
%\setcounter{secnumdepth}{2} % sections and subsections will be numbered
%\setcounter{secnumdepth}{3} % sections, subsections and subsubsections will be numbered

%% TrueType font for code. use \codeword{} around sections of code in your document
\NewDocumentCommand{\codeword}{v}{
  \texttt{\textcolor{black}{#1}}
}

% Create \thickhline command for tables (creates thick horizontal line)
\makeatletter
\newcommand{\thickhline}{%
  \noalign {\ifnum 0=`}\fi \hrule height 1pt
  \futurelet \reserved@a \@xhline
}
% Create " command for tables (creates thick vertical line)
\newcolumntype{"}{@{\hskip\tabcolsep\vrule width 1pt\hskip\tabcolsep}}
\makeatother


\pgfplotsset{compat=1.16}

% Create new column type for tabularx environment (Creates centred auto-width column)
\newcolumntype{Y}{>{\centering\arraybackslash}X}
